spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my_spring_batch?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: mysql
  batch:
    initialize-schema: always
    # job:
    #   names: conditionalStepLogicJob
