spring:
  batch:
   job:
     enabled: true
   initialize-schema: always
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my_spring_batch?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: mysql
    # initialization-mode: always
    continue-on-error: true
    platform: mysql
