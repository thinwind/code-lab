{
  "scripts": {
    "electron:build": "npx vue-cli-service electron:build",
    "electron:serve": "npx vue-cli-service electron:serve",
    "postinstall": "electron-builder install-app-deps",
    "postuninstall": "electron-builder install-app-deps"
  },
  "main": "background.js",
  "dependencies": {
    "@vue/cli": "^4.5.14"
  },
  "devDependencies": {
    "electron": "^13.0.0",
    "electron-devtools-installer": "^3.1.0",
    "vue-cli-plugin-electron-builder": "~2.1.1"
  }
}
